[package]
name = "openwa-debug-proto"
version = "0.1.0"
edition = "2021"
description = "Wire protocol types for OpenWA debug server"

[dependencies]
serde = { version = "1", features = ["derive"] }
rmp-serde = "1"
