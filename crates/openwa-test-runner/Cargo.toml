[package]
name = "openwa-test-runner"
version = "0.1.0"
edition = "2021"
description = "Headless replay test runner with concurrent execution"

[[bin]]
name = "openwa-test"
path = "src/main.rs"

[dependencies]
chrono = "0.4"
openwa-config = { path = "../openwa-config" }
