[package]
name = "openwa-lib"
version = "0.1.0"
edition = "2021"
description = "Shared library for calling WA.exe functions with typed wrappers"

[dependencies]
openwa-types = { path = "../openwa-types" }
